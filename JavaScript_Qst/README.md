// lastIndexOf() - 
//  it takes 2 param first str and searchInx 
// if you want check more safe so add typeOf condition
// if searchInx will fidn it return value
// if searchInx will not found it return -1
// it start form right to left

// indexOf() -
// also do the same work like lastIndexOf it take 2 param but it start from left to right


find()

indexFind()

Array.prototype.findIndex